# Folders starting with "__" — archives, bundles, scraped/downloaded mods, packages
__*/
__build*/

# Dotfiles & IDE config (user rule: ignore anything starting with ".")
.*

# ...except keep .gitignore itself tracked
!.gitignore