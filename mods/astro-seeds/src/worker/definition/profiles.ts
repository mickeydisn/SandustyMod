/**
 * Back-compat shim — prefer `profileCatalogue.ts` (grouped specs) +
 * `elementProfileFactory.ts` (generic builder).
 */
export * from "./profileCatalogue.ts";
export * from "./elementProfileFactory.ts";
